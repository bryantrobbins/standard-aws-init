# Default r10k configuration (from r10k Getting Started guide)
# BTR: Starting with this, but adapting for local puppet runs for now
# The location to use for storing cached Git repos
:cachedir: '/opt/puppet/r10k/cache'

# A list of git repositories to create
:sources:
  :standard:
    remote: 'github.com/bryantrobbins/standard-aws-control'
    basedir: '/etc/puppet/code/environments'
